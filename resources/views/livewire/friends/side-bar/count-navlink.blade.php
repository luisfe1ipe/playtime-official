<div>
    @if ($count > 0)
        <span
            class="flex items-center justify-center text-xs font-bold rounded-full text-primary-500 size-5 bg-primary-500/30">
            {{ $count }}
        </span>
    @endif
</div>
